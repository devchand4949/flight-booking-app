import 'package:flutter/material.dart';

class AppColors {

  // Main Blue
  static const Color primaryBlue = Color(0xFF457CE9);

  // Light Blue
  static const Color lightBlue = Color(0xFF87AAEE);

  // Background
  static const Color scaffoldBg = Color(0xFFE0E4ED);

  // Card Background
  static const Color cardBg = Color(0xFFF8F9FB);

  // White
  static const Color white = Color(0xFFFFFFFF);

  // Black
  static const Color black = Color(0xFF000000);

  // Dark Text
  static const Color textDark = Color(0xFF2E2E32);

  // Light Text
  static const Color textLight = Color(0xFF9CA3AF);

  // Border / Divider
  static const Color border = Color(0xFFE5E7EB);

  // Chip Background
  static const Color chipBg = Color(0xFFF2F3F5);

  // Shadow Color
  static const Color shadow = Color(0x14000000);
}